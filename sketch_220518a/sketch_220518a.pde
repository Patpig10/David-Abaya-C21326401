

void setup()
{
  size(500,500);
  
}

void draw()
{
  
  
}
// lenght has to be random, by lenght = cirle+lines
// head is seperate
// boolean for gender so if(M=true then add tail) if(F=true then add cirlce in last circle) if(N then both F and M = true)
// use arrays
